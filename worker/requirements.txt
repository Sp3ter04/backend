# WhisperX worker — runtime dependencies.
#
# NOTE: WhisperX itself is installed with --no-deps because it pulls
# pyannote.audio -> lightning, and `lightning` is currently quarantined on
# PyPI (no installable distributions). We don't need diarization for
# alignment, so we install only the deps actually exercised by the
# alignment code path.
#
#     pip install -r requirements.txt
#     pip install --no-deps whisperx==3.8.5
#     SSL_CERT_FILE=$(python -c "import certifi;print(certifi.where())") \
#       python -c "import nltk; nltk.download('punkt_tab')"
#
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
python-dotenv==1.2.2
requests==2.33.1

# WhisperX runtime deps (resolved from `whisperx 3.8.5` minus pyannote chain)
torch==2.11.0
torchaudio==2.11.0
transformers==5.7.0
faster-whisper==1.2.1
ctranslate2==4.7.1
huggingface-hub==1.13.0
onnxruntime==1.25.1
tokenizers==0.22.2
numpy==2.4.4
pandas==3.0.2
nltk==3.9.4
soundfile==0.13.1
av==17.0.1
librosa==0.11.0
